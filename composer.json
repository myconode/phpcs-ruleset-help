{
    "name": "chrid046/whats-wrong-with-phpcs",
    "type": "project",
    "require": {},

    "config": {
      "preferred-install": "dist",
      "github-domains": ["github.com"]
    },

    "require-dev": {
      "squizlabs/php_codesniffer": "^2.6.2"
    },

    "scripts": {
      "test": "composer exec 'phpcs -snp --standard=./phpcs.xml --report=summary example.php'"
    }
}
